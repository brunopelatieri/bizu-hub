# Contracts — Spec 003

| Arquivo | Descrição |
|---------|-----------|
| `blog-search.openapi.json` | OpenAPI 3.0 para `GET /api/blog/search` |

Validação: `npm run typecheck` + checklist `quickstart.md`.
